servercommon
=============

This section provides documentation for the various components of the `gearsdk` project. Below, you will find detailed information about various header files used in the project.

header files
------------

The following header files are documented here:

1. `servercommon.hpp`
2. `qmongo.hpp`

servercommon
-------------

The `servercommon.hpp` file contains the definition and implementation of the `servercommon` class, which provides common functionality for the server side of the project.

.. doxygenfile:: servercommon.hpp
   :project: gearsdk

qmongo
------

The `qmongo.hpp` file provides the `qmongo` class, which manages the connection and interaction with a MongoDB database.

.. doxygenfile:: qmongo.hpp
   :project: gearsdk