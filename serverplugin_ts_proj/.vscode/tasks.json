{
	"version": "2.0.0",
	"tasks": [
	{
		"label": "build:debug",
		"type": "shell",
		"command": "source ~/.nvm/nvm.sh && npm run clean && npm run build:debug && npm run watch",
		"isBackground": true,
		"problemMatcher": [
			"$tsc-watch"
		],
		"group": {
			"kind": "build",
			"isDefault": true
		},
		"options": {
			"env": {
				"PATH": "~/.nvm/versions/node/v16.16.0/bin:${env:PATH}"
			}
		}
	},
	{
		"label": "build:release",
		"type": "shell",
		"command": "source ~/.nvm/nvm.sh && npm run clean && npm run build:release && npm run start:release",
		"isBackground": false,
		"group": {
			"kind": "build"
		},
		"options": {
			"env": {
			"PATH": "~/.nvm/versions/node/v16.16.0/bin:${env:PATH}"
			}
		}
	}
	]
  }
  